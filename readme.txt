Метод ShowViewContent позволяет установить выводимый контент для функции AddViewContent:

<?$APPLICATION->ShowViewContent('drawDecorations');?> - при опции "PHP(Cache)+ js" необходимо вставить вызов этой функции после открывающего тега <body>